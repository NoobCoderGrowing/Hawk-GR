# Kuaisearch-KAE 复现工作复盘拆解

## 一、实体合并

因为受限于现有经济型大模型的学习推理能力（如 Bart），生成式召回的 SID 一般只有 4~6 位，而通常一个命名实体模型能够识别的实体少说都在二十个以上。按照 6 位 SID 的设定，这二十个实体必须被削减或合并为 6 个组别，于是如何合并的标准就是一个问题，也是 Onesearch-KAE 这篇论文的一个要点。下面谈一谈论文中的做法。

### 1.1 熵

论文将每一个实体类定义为一个二元随机变量，一个实体类 X 所包含的信息（熵）可以表示为：

```
H(X) = − p·log₂p − (1−p)·log₂(1−p)
```

其中 p 是实体类 X 在所有商品中出现的概率。这个函数的图像呈一个倒拱门型，p 取 0.5 时 H(X) 包含的信息最大，越接近 0 和 1 包含的信息越小——可以理解为越确定发生的事件所包含的信息越小。

### 1.2 互信息

知道如何定义一个实体类的信息量后，我们自然地可以想到：如果两个实体类所包含的信息高度相似，那么这两个实体类就可以考虑被合并。于是论文又定义了互信息的计算公式：

```
MI(X, Y) = Σ_{x∈X, y∈Y} p(x,y) · log( p(x,y) / (p(x)·p(y)) )
```

这个公式右半部分 log 里，分子衡量的是事件实际同时出现的概率，分母衡量的是假设事件独立时同时出现的概率；它们的比值体现了事件之间独立的程度，比值越大，事件之间关联性越大。

### 1.3 信息损失

是否包含信息越相似的实体类就应该被合并呢？论文的回答是否定的：文中认为应该合并那些**合并后信息损失最小**的实体类，于是就有了下面这个信息损失公式：

```
IL(X, Y) = ½(H(X) + H(Y)) − MI(X, Y)
```

可以理解为，两个实体类的自信息相加减去两个实体类的互信息，剩下的就是损失的信息了。

拿到这三个公式之后就可以开始合并实体类：每次合并 IL 损失最小的两个类别，重新计算合并后实体类的熵，再依次迭代合并，直到最终类别数量为 6，对应 6 位 SID。但是在进行合并之后会发现，6 个大类中会有个别大类包含很多小的实体类，而剩余大类则只有 1~2 个小类构成，分布很不均匀。这是因为，很多类之间因为毫无关联，对应的 MI 非常小（≈0），导致 `IL(X, Y) = ½(H(X)+H(Y)) − MI(X, Y)` 的结果非常大，而每次合并都是 IL 最小的两个类在一起合并，极端情况下会出现下面这种情况：

> **【配图待补】** 合并失衡的示意（原稿此处为空白）

### 1.4 正则化

所以论文中引入了 4 个正则化器：

| 正则器 | 规则 | 作用 |
|---|---|---|
| 组内熵同质性 | 高 H 和高 H 的合，低 H 和低 H 的合 | 防止高熵类型被「吸」进低熵大簇 |
| 激活率同质性 | p 接近的类别优先合并 | — |
| 语义一致性 | 人为规定哪些类型天然是一组 | — |
| 组大小软上限 | 超过阈值时增加惩罚 | — |

加上这 4 个约束后，合并路径完全不同，最终得到均衡的 ECOM6。

下面是我用开源电商命名实体模型在 Kuaisearch 商品数据上应用上述规则合并实体类的结果：

> **【配图待补】** 合并结果（原稿此处为空白）

## 二、槽设计

在把 43 个实体类分为 6 个大类后，我们要给每个大类进行 SID 划分。对于每一个 SID 来说，它相当于一个槽，一个槽内可以对应一个实体词或多个（注意不是实体类别）。论文中将一个 SID 内的槽划分为了三种：热门槽、聚类槽和可编辑槽。

### 2.1 热门槽

根据 recall 数据对热门实体词的曝光量，将每个大类中的实体词从高到低排序。我的做法是 1~1024 的槽位分配给热门词，其中 0 为默认槽，其代表的意义是当模型无法为当前商品生成对应位置的 SID 时，默认分配 0。

### 2.2 聚类槽

聚类槽是用来对应曝光量排位 1025 之后的实体词的。我的做法是定义 962 个聚类，先对 1025 之后的实体词用 bge-large-zh-v1.5 模型进行词向量嵌入，再用 K-means 算法将长尾实体词分配到 962 个槽中。

### 2.3 可编辑槽

正常的垂直搜索引擎中都会有一个坑位系统，这是为了赋能运营将某个商品词在搜索结果中固定排序到某一个位置。为了在生成式检索系统中保留这个功能，我们需要预留一定槽位，在后续的四阶段 SFT 训练中形成可编辑预留槽映射。我的做法是将每个 SID 段位的 1987~2046 设置为预留槽。

### 2.4 词表更新

完成槽位设计后，我们需要更新后续四阶段 SFT 训练的词表，即将每个 SID 如 `a_1` 加入 Bart 模型的 token 词表当中。

## 三、四阶 SFT 训练

### 3.1 第一阶段：双向属性-SID 对齐

目标是让模型学习属性与 SID 的对应关系。样例训练数据如下：

```text
正向
  input : 属性是[裤]，类别是[产品核心]，SID是：
  output: <a_1>

逆向
  input : SID是[<a_1>]，类别是[产品核心]，属性是：
  output: 裤
```

### 3.2 第二阶段：内容对齐（Content Alignment）

目标是让模型学习 query 和 query_SID、以及 item 与 item_SID 的对应关系。第二阶段训练有四种训练类别：

**① Query → SID**：输入用户查询文本，输出该查询的 SID。

```json
{"input": "广场舞服装女高档洋气", "output": "<a_304><b_1><c_13><d_0><e_0><f_197>"}
```

**② SID → Query**：输入 SID，输出对应的查询文本。

```json
{"input": "<a_304><b_1><c_13><d_0><e_0><f_197>", "output": "广场舞服装女高档洋气"}
```

**③ Title → SID**：输入商品标题，输出该商品的 SID。

```json
{"input": "【香榭丽舍】高级静奢风法式长款遮肉显瘦进口貂皮大衣", "output": "<a_120><b_0><c_58><d_84><e_1244><f_0>"}
```

**④ SID → Title**：输入 SID，输出对应的商品标题。

```json
{"input": "<a_120><b_0><c_58><d_84><e_1244><f_0>", "output": "【香榭丽舍】高级静奢风法式长款遮肉显瘦进口貂皮大衣"}
```

### 3.3 第三阶段：协同共现

目标是建立 Query-SID 到 Item-SID 的路由，并训练可编辑槽的映射。

论文是在第四阶段个性化训练中才训练可编辑槽的映射，因为缺乏相关的历史 query 数据，无法进行个性化训练，可编辑槽的训练被包括在第三阶段训练当中。注意可编辑槽不可以单独训练，单独训练会破坏模型正常的对其他任务的泛化能力；正确的训练方法是大量其他任务训练数据中夹杂少量的可编辑槽数据。

我的做法是 660 万条训练数据中夹杂了 3600 条可编辑槽的训练数据，比例大约是 `1 : 5×10⁻⁴`。论文中实际比例更小，但在实际训练中发现，只有将可编辑槽的训练数据提高到每个 slot 出现 10 次，映射才能达到 95% 的覆盖率。

对于一个具体的 (Query, Item) 行为对，Stage 3 会构造 4 个不同的训练样本（2 个在文本表面层，2 个在 SID 深层）：

**任务 A（表层）：Query 文本 → Item 标题文本**

```text
input : 广场舞服装女高档洋气
output: PYX-208艺尚舞女士新款裤子气质吉特巴广场舞拉丁舞裤
```

**任务 B（表层）：Item 标题文本 → Query 文本**

```text
input : PYX-208艺尚舞女士新款裤子气质吉特巴广场舞拉丁舞裤
output: 广场舞服装女高档洋气
```

**任务 C（SID 深层-正向）：Query 的 SID → Item 的 SID（核心路由）**

```text
input : <a_304><b_1><c_13><d_0><e_0><f_197>
output: <a_5><b_7><c_1><d_0><e_0><f_197>
```

**任务 D（SID 深层-反向）：Item 的 SID → Query 的 SID（强制对称性）**

```text
input : <a_5><b_7><c_1><d_0><e_0><f_197>
output: <a_304><b_1><c_13><d_0><e_0><f_197>
```

**任务 E（可编辑槽训练）：夹带可编辑槽的 Query 的 SID → 夹带可编辑槽的 Item 的 SID**

```text
input : <a_1987><b_500><c_0><d_16><e_21><f_0>
output: <a_1987><b_110><c_0><d_0><e_1><f_0>
```

注意这里第一个槽位 `a_1987` 是个可编辑槽。

## 四、Aho-Corasick（AC 自动机）

在我们完成可编辑槽的映射后，我们该怎么使用它呢？一个简单方法就是使用 AC 自动机。AC 自动机是一个字典树的结构，对于每一个字符子串它都可以 output 一个 SID；我们只需要在构建 AC 自动机的词表当中添加一条数据，如「冰箱」→「a_1987」，就可以让运营把「冰箱」这个关键词绑定到 1987 这个 a 类槽位上，再在坑位系统中对所有 a 类槽位为 1987 的商品进行排序就可以了。这样我们就做到了仅更新 AC 自动机的字典文件，就可以实时地对于新商品排序做调整。
